/* 8. Write a C++ Program to implement push and pop methods from stack using
template. */
#include<iostream>
#include<stack>
using namespace std;
template<class T>
int main()
{
    stack s;
    T a;
    for(int i=0;i<5;i++)
    {
        cin>>a
        s.push(a);
    }
    return 0;
}
